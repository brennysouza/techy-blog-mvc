<article class="postSection">
    <div class="title">
       <a href="/post/{{id}}">{{title}}</a>
    </div>
    <div class="body">
        {{post_text}}
    </div>
    <div class="meta">
        <strong>Created by {{user.username}}, {{format_date created_at}}</strong>
    </div>
</article>